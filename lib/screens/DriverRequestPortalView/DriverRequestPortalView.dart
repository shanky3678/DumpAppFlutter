import 'package:flutter/material.dart';

class DriverRequestPortalView extends StatelessWidget {
  const DriverRequestPortalView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Nothing yet."),
      ),
    );
  }
}
